namespace Rotation;

public static class ExNumber {
    public static float Max(params float[] pElements) =>
        pElements.Max();

    public static float Min(params float[] pElements) =>
        pElements.Min();
}